// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        jcenter()
        mavenCentral()

        maven {
            url 'https://dl.bintray.com/gdev2018/tgjni'
        }
    }

    dependencies {
//        classpath 'com.android.tools.build:gradle:3.2.1'  //for AS_3.2
//        classpath 'com.android.tools.build:gradle:3.5.3'  //for AS_3.4
//        classpath 'com.android.tools.build:gradle:4.1.1'  //for AS_4.1
        classpath 'com.android.tools.build:gradle:4.2.0-alpha15'  //for AS_4.2
        classpath 'com.google.gms:google-services:4.3.4'

//        classpath 'com.frogermcs.androiddevmetrics:androiddevmetrics-plugin:0.6'
//        classpath 'org.jacoco:org.jacoco.core:0.8.1'

//        classpath 'com.novoda:bintray-release:0.9'    // 4public custom module in maven/jcenter
//        classpath 'com.novoda:bintray-release:0.9.2'
        classpath 'com.github.panpf.bintray-publish:bintray-publish:1.0.0'
    }
}

//report will be here - https://scans.gradle.com/s/proqsyyqkcoju/performance/configuration?openScriptsAndPlugins=WzIsMF0
//plugins {
//    // Check for the latest version here: https://plugins.gradle.org/plugin/com.gradle.build-scan
//    id("com.gradle.build-scan") version "1.16"
//}
//
//buildScan {
//    setTermsOfServiceUrl("https://gradle.com/terms-of-service")
//    setTermsOfServiceAgree("yes")
//
//    // Keep this while optimizing your builds,
//    // but then remove it once you're done — unless you want to share all future builds with Gradle.
//    // If removed, you can still get build scans by using the --scan option.
//    publishAlways()
//}

repositories {
    google()
}

allprojects {
    repositories {
        google()
        jcenter()

        maven {
            url 'https://dl.bintray.com/gdev2018/tgjni'
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}